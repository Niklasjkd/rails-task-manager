<h1>Tasks</h1>

<ul>
<% @tasks.each do |task| %>

<li> <%= link_to task.title, task_path(task) %> - <%= task.completed %> <%= link_to "DELETE", task_path(task), method: :delete %></li>

<% end %>

</ul>

<%= link_to "ADD TASK", tasks_new_path %>
